# =============================================================================
# DRL Quant Trading — SAC-Dirichlet Framework
# Python >= 3.11 recommended
# =============================================================================

# --- Core numerical / data ---
numpy>=1.24
pandas>=2.0
scipy>=1.11          # regime_hmm (logsumexp), benchmarks (optimize.minimize)
scikit-learn>=1.3    # regime_hmm (KMeans init for HMM)

# --- Deep learning ---
torch>=2.1           # SAC agent, Dirichlet policy, MPS/CUDA detection

# --- Market + macro data ---
yfinance>=0.2.40     # load_market_data (SPY, TLT, GLD, VNQ, BTC-USD)
TA-Lib>=0.4.28       # RSI features (requires C library; see note below)

# --- Plotting ---
matplotlib>=3.7      # all figures (thesis_style, plotting.py, notebook)

# --- Terminal UI ---
tqdm>=4.65           # training progress bar in agent.learn()

# --- Notebook (optional, for analysis/models_analysis.ipynb) ---
jupyter>=1.0
ipykernel>=6.25

# =============================================================================
# NOTE on TA-Lib:
# The Python package `TA-Lib` wraps the native C library. On Windows, install
# the pre-built wheel or use conda:
#   conda install -c conda-forge ta-lib
# On macOS:       brew install ta-lib   then  pip install TA-Lib
# On Linux:       apt-get install libta-lib-dev   then  pip install TA-Lib
# =============================================================================
